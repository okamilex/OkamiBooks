

<h2 class="col-lg-12">
    {{user.Email}}
</h2>


<table class="table">
    <thead>
        <tr>
            <th>{{transaction.Maedals}}</th>


        </tr>
    </thead>
    <tbody>
    <tr>
        <td ng-if="user.Medal1">
            saf
        </td>

    </tr>
    <tr>
        <td ng-if="user.Medal2">
            safsaf
        </td>

    </tr>
        <tr>
            <td ng-if="user.Medal3">
                saf
            </td>

        </tr>
    </tbody>
</table>






<div class="list-group col-lg-12">
    <a class="list-group-item active">
        <h4 class="list-group-item-heading">
            Books
        </h4>
    </a>
    <div ng-repeat="book in books" class="panel panel-default">
        <div class="panel-heading">
            {{book.Name}}
            <a ng-if="permission" href="#/edit" type="button" class="btn btn-link btn-mini pull-right"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
            <a href="#/read" type="button" class="btn btn-link btn-mini pull-right"><span class="glyphicon glyphicon-book" aria-hidden="true"></span></a>
        </div>
        <div class="panel-footer">
            dsgdsg

            <button type="button" class="btn btn-link btn-mini pull-right"><span class="glyphicon glyphicon-comment" aria-hidden="true"></span> <span class="badge">{{book.CommentCount}}</span></button>
        </div>
    </div>
</div>

<button ng-click="startEditNewBook()" ng-if="permission" type="button" class="btn btn-link btn-mini pull-right"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> </button>

